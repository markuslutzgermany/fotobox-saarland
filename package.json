{
  "name": "fotobox-saarland",
  "version": "0.1.0",
  "private": true,
  "description": "fotobox.saarland — Static Site mit Eleventy auf Cloudflare Pages",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
